import Foundation
import SpriteKit

// BallDamageSystemDelegate conformance is declared in the main StarfieldScene class
extension StarfieldScene {
    // Optional hooks retained for future use; onDamage now lives on BlockBall
}

